# .env.example — コピーして .env を作成し、強力な値に変更してください。
# .env は絶対にコミットしないこと（.gitignore 済み想定）。

POSTGRES_USER=hr_app
# 強力なランダム値に変更（例: openssl rand -base64 24）
POSTGRES_PASSWORD=change_me_use_a_strong_secret
POSTGRES_DB=hr_saas
