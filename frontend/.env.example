# Go API base URL (no trailing slash)
# Default: http://localhost:8080
API_URL=http://localhost:8080

# Node.js server port for the React Router frontend
PORT=3000

# Session cookie name (must match the Go API SESSION_COOKIE_NAME)
SESSION_COOKIE_NAME=hr_session
